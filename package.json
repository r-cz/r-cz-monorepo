{
  "name": "r-cz-monorepo",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "cf-build-main": "cd apps/main && bun run build",
    "cf-build-tools": "cd apps/tools && bun run build",
    "test": "bunx playwright test tests/main/main-site.spec.ts tests/tools/",
    "test:main": "bunx playwright test tests/main/main-site.spec.ts",
    "test:tools": "bunx playwright test tests/tools/",
    "test:ui": "bunx playwright test --ui",
    "test:theme": "bunx playwright test tests/main/main-site.spec.ts:32 tests/tools/theme.spec.ts",
    "test:generate-report": "bunx playwright show-report",
    "clean": "bun ./scripts/clean.js",
    "clean:dry": "bun ./scripts/clean.js --dry-run",
    "clean:force": "bun ./scripts/clean.js --force"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "@tailwindcss/postcss": "^4.0.9",
    "eslint": "^8.57.1",
    "prettier": "^3.5.2",
    "turbo": "^2.4.4"
  },
  "packageManager": "bun@1.2.3",
  "engines": {
    "node": ">=18.0.0"
  }
}
